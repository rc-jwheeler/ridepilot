= render 'shared/highlight_active_tab_js', is_primary_nav: true, tab_class: 'reports'

= render partial: 'list', locals: {report_id: @report.id}

= render partial: 'header', locals: {report: @report}

.filter-form
  = render partial: 'reporting/filters/form', locals: {report: @report, filter_groups: @filter_groups}
