<%= render 'shared/highlight_active_tab_js', is_primary_nav: true, tab_class: 'trips' %>
<div id="page-header">
  <h1>View Trips</h1>
  <%= render :partial => 'weekday_select' %>
  <div class="clearfix"></div>
</div>

<div id="calendar" data-start-time="<%= @week_start.to_i * 1000 %>"></div>

<%= render :partial => 'list' %>

<hr />

<%= link_to 'New Trip', new_trip_path, :class=>'action-bttn new' %>

<%= render 'javascript' %>

