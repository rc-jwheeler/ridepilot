#page-header.row
  .col-sm-12.col-md-6
    %h1= translate_helper("new_provider_heading")
  .col-sm-12.col-md-6
    - if can? :manage, @provider
      .pull-right
        %button#provider_form_submit.btn.action-button
          = translate_helper("save")
        = link_to translate_helper("cancel"), providers_path, class: "btn action-button"

= render 'form' 
