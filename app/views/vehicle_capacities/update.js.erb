$('#modal-dialog').modal('hide');
var $new_row = $("<%= j (render @vehicle_capacity) %>").hide();
$('#capacities_table .vehicle_capacity-<%= @vehicle_capacity.id %>').slideUp('fast').replaceWith($new_row);
$new_row.slideDown('slow');
