<div id="page-header">
  <h1>Editing Trip</h1>
</div>

<%= render :partial => 'weekday_select' %>

<div id="calendar" data-start-time="<%= @week_start.to_i * 1000 %>"></div>

<%= render 'list', :display => "none" %>

<%= render 'form' %>

<%= render 'javascript' %>

