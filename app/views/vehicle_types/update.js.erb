$('#modal-dialog').modal('hide');
var $new_row = $("<%= j (render @vehicle_type) %>").hide();
$('#vehicle_types_table .vehicle_type-<%= @vehicle_type.id %>').slideUp('fast').replaceWith($new_row);
$new_row.slideDown('slow');
