<div id="page-header" class="clearfix">
  <h1><%= translate_helper("change_email_heading") %></h1>
</div>

<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2>
      <h2><%= translate_helper("user_form_error_message", count: @user.errors.count) %></h2>
    </h2>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @user, :url => change_email_path(id: @user.try(:id)) do |f| %>
<fieldset>
  <ol class="section first">
    <li>
      <%= f.label :current_email, translate_helper("current_email") %>
      <%= @user.email %>
    </li>  
    <li>
      <%= f.label :email, translate_helper("new_email") %>
      <%= f.email_field :email, class: 'form-control' %>
    </li>
  </ol>
  <div class="actions">
    <%= f.submit translate_helper("change_email_submit") %>
  </div>
</fieldset>
<% end %>