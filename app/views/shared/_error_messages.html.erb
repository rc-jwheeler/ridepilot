<% if target.errors.any? %>
  <div class="error_explanation">
    <h2><%= translate_helper("generic_error_message", count: @target.errors.count)%></h2>
    <ul>
      <% target.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
