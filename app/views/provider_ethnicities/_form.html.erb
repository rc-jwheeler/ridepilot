<%= form_for(@provider_ethnicity) do |f| %>
  <% if @provider_ethnicity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@provider_ethnicity.errors.count, "error") %> prohibited this provider_ethnicity from being saved:</h2>

      <ul>
      <% @provider_ethnicity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>

    <ol class="section first">
      <li>
        <%= f.label :name %>
        <%= f.text_field :name %>
      </li>

    </ol>

    <div class="actions">
      <%= f.submit %>
    </div>

  </fieldset>
<% end %>
