<div id="page-header">
  <h1><%= translate_helper("new_run_heading") %></h1>
</div>

<%= form_for @run do |f| %>
  <%= render "form", :f => f %>
  <div class="actions">
    <%= f.submit translate_helper("new_run_submit") %>
  </div>
<% end %>
