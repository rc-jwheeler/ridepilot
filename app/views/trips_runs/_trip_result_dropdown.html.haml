%span.dropdown.dispatch-action.trip_results_dropdown{style: 'margin-bottom: 5px;'}
  %a.dropdown-toggle.non_jump_top{href: '#', type: 'button', style: 'color: white; font-size: small;', data: {toggle: 'dropdown'}} 
    Set trips as
    %i.fa.fa-ellipsis-h
    %span.caret
  %ul.dropdown-menu    
    - TripResult.order(:name).pluck(:id, :name).each do |result_id, label|
      %li
        %a.trip_result_option.non_jump_top{href: '#', data: {trip_result_id: result_id} }
          = label